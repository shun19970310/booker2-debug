<!--rails routesで /books/:book_id/となっており、通常は/books/:idのようになっているが、この場合はpathの後ろにurlを指定する必要がある。-->
<!--(book_id: book.id):model:[book, book_comment]のbookに紐づくidをurl内のbook_idに代入している-->
<!--コメント送信後に前のページに遷移しなかった←routes.rbで誤ったルーティングを記述していたので、正しいルーティングを設定-->
<!--その結果、book_comments#createのパスがbook_book_commentsに変わったので修正-->
<!--form_withで、JavaScriptのAjaxという技術を使用した非同期通信を行なうためにlocal: falseというオプションを記述-->
<%= form_with(model:[book, book_comment],url: book_book_comments_path(book_id: book.id), local: false) do |f| %>
  <!--create.jsファイルでフォームを送信したあとにフォームの値を空にするため、class名にcomment-textareaを追記-->
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに", class: "w-100 comment-textarea" %>
  <%= f.submit "送信する", class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
<% end %>